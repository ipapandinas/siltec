export interface IImage {
  attributes: {
    alternativeText: string | null;
    url: string;
  };
}

export interface IDocument {
  attributes: {
    alternativeText: string | null;
    url: string;
  };
}
